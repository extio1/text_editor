name: C++ google test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: mkdir build; cd build
    - run: ls -la
    - run: cmake ..
    - run: cmake --build .
    - run: ./vector_test
