#include "../../include/glyphs/utils/point.h"
#include "../../include/glyphs/composition.h"

explicit Composition::Composition(const int x, const int y, const int width, const int height):
        Glyph(x, y, width, height) {
    // TODO
}

size_t Composition::GetGlyphPosition(const GlyphPtr& glyph) {
    // TODO
}
Composition::GlyphPtr Composition::Find(const Point& point) {
    // TODO
}
